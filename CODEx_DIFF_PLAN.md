# CODEx_DIFF_PLAN.md — финальный апгрейд Skillra HSE PDA

Repo: `vsyntot/skillra_hse_pda`

Context:
- ТЗ курса «Python для анализа данных» (этапы 0–4, структуру ноутбука, визуализации, текстовые выводы).
- Продуктовый план Career & Job Market Navigator — Skillra (карта рынка, топ‑скиллы, персоны, карьерные сценарии).
- Словарь признаков hh-датасета + командный ноутбук с EDA-идеями.

Цель:
1. Довести кодовую базу до аккуратного, устойчивого состояния (уровень продакшн‑EDA проекта).
2. Максимально закрыть ТЗ курса (этапы 0–4 + аккуратное оформление ноутбука).
3. Втащить идеи из группового ноутбука в единый, чистый пайплайн и ноутбук.
4. Сделать проект удобным для быстрых проверок (скрипты/валидаторы) и презентации инвесторам/академическому руководителю.

Детализированный план изменений описан в пользовательском сценарии (cleaning.py → io.py → features.py → eda.py → viz.py → personas.py → scripts → notebook → README), включая:
- устойчивую обработку булевых колонок и `salary_gross`;
- добавление новых фичей (vacancy_age_days, стек‑агрегаты, текстовые признаки и т.д.);
- расширенные EDA и визуализации (heatmap навыков, бенефиты, soft‑skills);
- продуктовый слой с персонами и skill-gap;
- скрипты запуска/валидации пайплайна и выполнения ноутбука;
- структурирование ноутбука по этапам ТЗ с выводами и вкладом команды;
- обновление README с инструкциями и связью с продуктом Skillra.

Definition of Done:
- `python scripts/run_pipeline.py` и `python scripts/validate_notebook.py` выполняются без ошибок;
- ноутбук `notebooks/01_hse_project.ipynb` успешно выполняется целиком (Restart & Run All) и покрывает этапы 0–4 с выводами и секцией вкладов;
- README описывает установку, запуск пайплайна, валидацию ноутбука и продуктовый контекст.
